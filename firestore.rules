rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function isAllowedUser() {
      return request.auth != null
        && request.auth.uid == "REPLACE_WITH_ALLOWED_UID";
    }

    match /{document=**} {
      allow read, write: if isAllowedUser();
    }
  }
}
