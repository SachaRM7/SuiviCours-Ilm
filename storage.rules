rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    function isAllowedUser() {
      return request.auth != null
        && request.auth.uid == "YffF7VLfMoXrgo8TTZzlKGwHPIu1";
    }

    match /{path=**} {
      allow read, write: if isAllowedUser();
    }
  }
}
